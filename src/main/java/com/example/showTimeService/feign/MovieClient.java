/*
package com.example.showTimeService.feign;

import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;

@FeignClient(name = "movie-service", url = "http://localhost:8081/api/movies") // Adjust URL as needed
public interface MovieClient {

    @GetMapping("/{movieId}/exists")
    boolean checkIfMovieExists(@PathVariable int movieId);
}
*/
